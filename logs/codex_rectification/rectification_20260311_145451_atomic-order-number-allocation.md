# Codex Rectification Record

## Basic Information
- Time: 2026-03-11 14:54:51 +08:00
- Executor: Codex
- Summary: Upgrade order number allocation to a database-backed atomic counter.

## Trigger
- Source: user report
- Context: After the prior review-fix pass, the user asked for a detailed explanation of the order number race and then requested the stronger fix.

## Defect
- Description: Order numbers were generated by reading the latest matching order number from the main table and incrementing it in application code.
- Impact: Concurrent create requests could compute the same next number and collide on the unique constraint, especially under burst traffic.

## Correction
- Files Updated: `database.py`
- Change Summary: Added a dedicated order-number sequence table during bootstrap; introduced `generate_order_no_atomic()` to allocate numbers via database row update under `UPDLOCK, HOLDLOCK`; switched order creation to the new allocator while keeping duplicate-key retry as a fallback.

## Verification
- Performed: `C:\Users\charl\AppData\Local\Programs\Python\Python312\python.exe -m py_compile web_server.py database.py config\settings.py utils\feishu_api.py`
- Result: Passed.

## Notes
- The first request for a new `(prefix, date)` key still relies on inserting the counter row, with the existing duplicate-key retry path acting as the fallback if that row is initialized concurrently.
