# 任务：优化 Rules 文件夹

## 背景

当前 `rules/` 目录下共有 8 个规则文件，经过评审发现存在冗余、矛盾和可操作性不足的问题。
本任务要求你对这些文件进行系统性优化，**不改变核心规则内容，只改善结构、消除冗余、补全缺失**。

---

## 执行前置检查

在开始任何修改前，必须完整读取以下所有文件：

- `rules/README.md`
- `rules/00_core.md`
- `rules/01_workflow.md`
- `rules/02_debug.md`
- `rules/03_hotfix.md`
- `rules/04_frontend.md`
- `rules/05_task_convention.md`
- `rules/06_testing.md`

---

## 优化任务清单

按优先级顺序执行，**每完成一项后输出确认，再继续下一项**。

---

### 🔴 P0 — 立即处理（规则一致性）

#### T1｜统一评分门槛的 Source of Truth

- **问题**：评分门槛数字同时出现在 `02_debug.md` 和 `05_task_convention.md`，存在两个 Source of Truth
- **操作**：
  1. 保留 `02_debug.md` 中的评分门槛表为唯一定义
  2. 将 `05_task_convention.md` 中的重复评分内容替换为引用说明：
     > 评分门槛定义见 `02_debug.md` — 评分维度章节，本文件不重复定义。
- **验证**：确认两文件中评分数字只出现一次

---

#### T2｜明确 HOTFIX 与 8D 的关系

- **问题**：`03_hotfix.md` 与 `02_debug.md` 流程边界模糊，不清楚是否可嵌套
- **操作**：在 `03_hotfix.md` 顶部"红线"章节之前，新增以下声明块：

  ```markdown
  ## 与 8D 协议的关系 / Relationship with 8D Protocol

  HOTFIX 是 8D 的**紧急快速通道**，适用于生产环境紧急问题。

  | 对比项 | HOTFIX | 8D |
  |--------|--------|----|
  | 适用场景 | 生产紧急，需立即止血 | 非紧急 Bug，需根因分析 |
  | 流程长度 | 5步（简化） | 8步（完整） |
  | 审核节点 | tester 冒烟测试 | reviewer 评分 × 3 |
  | 事后要求 | 必须在 3 个工作日内补全 8D D4-D8 | 完整执行 |

  > ⚠️ 若 HOTFIX 完成后未在 3 个工作日内补全 8D，禁止归档。
  ```

- **验证**：确认 `03_hotfix.md` 包含上述对比表

---

#### T3｜修正 test_coverage 门槛为可量化标准

- **问题**：`test_coverage` 要求"必须满分（= 0.20）"，在实际项目中不可达，且与其他维度逻辑不一致
- **操作**：在 `02_debug.md` 评分维度表中，将 `test_coverage` 的最低门槛改为 `≥ 0.16`，并在表格下方新增注释：

  ```markdown
  > **test_coverage 评分参考**：
  > - 1.0 分：新增代码覆盖率 ≥ 80% 且覆盖所有修改路径
  > - 0.8 分：新增代码覆盖率 ≥ 60%
  > - 0.6 分：仅有手动验证记录，无自动化测试
  > - 0 分：无任何测试
  ```

- **验证**：确认门槛数字已更新，注释已添加

---

### 🟡 P1 — 近期处理（可维护性）

#### T4｜UTF-8 规则去重

- **问题**：UTF-8 要求出现在 `00_core.md`、`03_hotfix.md` 共 2 处
- **操作**：
  1. 保留 `00_core.md` 中的定义不变
  2. 将 `03_hotfix.md` 红线第4条替换为：
     > **UTF-8 铁律**: 见 `00_core.md` — 编码章节，所有文件操作必须遵守。
- **验证**：确认 `03_hotfix.md` 不再包含重复的 UTF-8 代码示例

---

#### T5｜合并"零假设"原则

- **问题**：`00_core.md`（禁止占位符）、`03_hotfix.md`（零假设验证）、`06_testing.md`（零假设验证）语义重叠
- **操作**：
  1. 在 `00_core.md` 的"代码完整性"章节扩充为：

     ```markdown
     ## Zero-Assumption Policy（零假设原则）

     1. **禁止占位符代码**：AI 严禁生成 `pass`、`TODO`、`...` 等未实现代码
     2. **禁止假设验证**：操作前后必须实际运行验证，不假定代码一写就对
     3. **禁止假设结构**：测试和实现必须基于真实代码结构，不假设接口存在
     ```

  2. `03_hotfix.md` 红线第3条替换为：
     > **零假设验证**: 见 `00_core.md` — Zero-Assumption Policy。
  3. `06_testing.md` 原则章节中的"零假设验证"替换为：
     > **零假设验证**: 见 `00_core.md` — Zero-Assumption Policy。

- **验证**：确认三处语义统一，只有 `00_core.md` 包含完整定义

---

#### T6｜README 增加流程选择决策树

- **问题**：ADP / 8D / HOTFIX 三个流程没有明确的触发判断入口
- **操作**：在 `README.md` 文件结构表格之后，新增以下章节：

  ```markdown
  ## 流程选择决策树 / Workflow Decision Tree

  收到任务时，按以下顺序判断应使用哪个协议：

  ```
  收到任务
   │
   ├─ 生产环境紧急故障？
   │   └─ YES → 🚑 HOTFIX SOP (03_hotfix.md)
   │
   ├─ 已有功能出现 Bug / 回归问题？
   │   └─ YES → 🐛 8D 协议 (02_debug.md)
   │
   ├─ 新功能 / 重构 / UI 迁移？
   │   └─ YES → 🔄 ADP 四阶段 (01_workflow.md)
   │
   └─ 纯测试任务？
       └─ YES → 🧪 测试规范 (06_testing.md)
  ```

  > ⚠️ 若任务横跨多个类型（如修 Bug 时发现需重构），**以最高优先级流程为主**，
  > 子任务拆分为独立编号单独执行。
  ```

- **验证**：确认 `README.md` 包含决策树章节

---

#### T7｜补全测试覆盖率具体数字

- **问题**：`06_testing.md` Pass Criteria 要求"覆盖率达标"但无具体数字
- **操作**：在 `06_testing.md` 的"Pass Criteria"章节，将覆盖率要求替换为：

  ```markdown
  ### 覆盖率最低要求 / Minimum Coverage Requirements

  | 测试类型 | 最低覆盖率 | 说明 |
  |---------|-----------|------|
  | 单元测试 | ≥ 80% | 针对新增/修改代码行 |
  | 集成测试 | ≥ 60% | 针对模块间接口 |
  | E2E 测试 | 覆盖全部 P0 路径 | 无覆盖率百分比要求 |
  ```

- **验证**：确认具体数字已写入文件

---

### 🟢 P2 — 补全缺失项

#### T8｜补全 README Skills 映射表

- **问题**：`README.md` 的 Skills 映射表缺少 `04_frontend.md`、`05_task_convention.md`、`06_testing.md` 的对应条目
- **操作**：找到 README 中的 Skills 映射表，补全缺失行，格式与现有行保持一致；
  若某规则文件无对应自动化 Skill，注明 `手动执行 / Manual`
- **验证**：确认映射表行数与规则文件数量一致

---

#### T9｜`00_core.md` 补充缺失规范

在 `00_core.md` 末尾新增以下两个章节（内容简洁，不超过 20 行）：

**章节 A — 依赖管理**
```markdown
## 依赖管理 / Dependency Management

- Python 依赖必须锁定版本：`requirements.txt` 使用 `==` 精确版本
- Node 依赖必须提交 `package-lock.json` 或 `pnpm-lock.yaml`
- 禁止在生产代码中使用 `latest` 或无版本约束的依赖
```

**章节 B — 敏感信息**
```markdown
## 敏感信息处理 / Secrets Management

- 禁止将密钥、Token、密码硬编码在任何代码文件中
- 所有敏感信息必须通过环境变量或 `.env` 文件注入
- `.env` 文件禁止提交到版本控制，必须在 `.gitignore` 中声明
```

- **验证**：确认两个章节已写入 `00_core.md`

---

## 执行约束

1. **禁止跳项**：按 T1 → T9 顺序执行，每项完成后输出 `✅ T{N} 完成` 再继续
2. **禁止删除核心规则**：只能新增、移动、替换为引用，不得删除任何业务规则内容
3. **禁止修改文件编号**：`00_core.md` ~ `06_testing.md` 文件名不变
4. **保持双语格式**：新增内容必须与现有文件的中英双语风格一致
5. **完成后输出变更摘要**：所有任务完成后，输出一份包含"修改文件 × 修改行数"的变更摘要表

---

## 完成标准

所有任务完成后，rules 目录应满足：

- [ ] 评分门槛只有一个 Source of Truth（`02_debug.md`）
- [ ] HOTFIX 与 8D 关系已明确声明
- [ ] `test_coverage` 门槛有可量化定义
- [ ] UTF-8 / 零假设 / 团队协议无跨文件重复定义
- [ ] `README.md` 包含流程选择决策树
- [ ] 测试覆盖率有具体百分比
- [ ] `00_core.md` 包含依赖管理和敏感信息章节