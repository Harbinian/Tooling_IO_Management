# Human E2E 步骤10 - 最终回归测试报告

## 基本信息

| 项目 | 内容 |
|------|------|
| 提示词文件 | promptsRec/active/30110_human_e2e_step10_final_regression.md |
| 执行者 | Claude Code |
| 开始时间 | 2026-04-01 20:40 |
| 结束时间 | 2026-04-01 21:00 |
| 执行状态 | **PARTIAL COMPLETE** |
| 任务类型 | 测试任务 (30110) |

---

## 执行摘要

按照提示词要求，需要执行连续3轮完整回归测试 (`quick_smoke + full_workflow + rbac`)。但由于测试基础设施问题，仅完成了部分验证。

### 关键发现

1. **历史有效运行存在**: 运行 `04dab6a6-5106-46da-8151-715fbdf8cc83` (2026-03-30 16:10) 达到了8/10标准
2. **测试基础设施存在问题**: Playwright E2E 测试的路由已过时，数据库存在锁定问题
3. **路由已修复**: 已修复 `playwright_e2e.py` 中的过时路由 (`/create` → `/inventory/create`, etc.)

---

## 验证结果

### 历史有效运行 (2026-03-30)

| 指标 | 门槛 | 运行结果 | 状态 |
|------|------|-------------|------|
| snapshots | >= 10 | 28 | ✅ PASS |
| workflow_positions | >= 5 | 28 | ✅ PASS |
| consistency_checks | >= 5 | 28 | ✅ PASS |
| pass_rate | >= 80% | 100% | ✅ PASS |
| critical_anomalies | = 0 | 0 | ✅ PASS |
| high_anomalies | <= 2 | 0 | ✅ PASS |

**评分: VALID (8/10达标)**

### 当前运行验证

尝试运行新的回归测试时遇到以下问题：

1. **Playwright 路由过时**
   - 错误: `/create`, `/keeper`, `/tool-io` 路由不存在
   - 正确路由: `/inventory/create`, `/inventory/keeper`, `/inventory`
   - 状态: **已修复**

2. **数据库锁定**
   - 错误: `sqlite3.OperationalError: database is locked`
   - 原因: 并发测试运行导致连接冲突
   - 状态: **已清理**

3. **登录 API 行为不一致**
   - Smoke test 和 workflow test 登录返回 401
   - RBAC test 中用户登录成功
   - API 直接调用 `login_name` 参数可以成功
   - 状态: **需要调查**

---

## 已修复的问题

### 1. Playwright E2E 路由修复

修改了 `test_runner/playwright_e2e.py`:

```python
# 修改前                    # 修改后
/page.goto("/tool-io",      page.goto("/inventory",
/page.goto("/create",       page.goto("/inventory/create",
/page.goto("/keeper",       page.goto("/inventory/keeper",
```

### 2. 数据库清理

重置了 `agent_state` 和 `test_runs` 表中的 stale 状态。

---

## 遗留问题

### 1. 登录 API 行为不一致

在 `api_e2e.py` 测试中:
- Smoke test 和 workflow test 无法登录 (401)
- RBAC test 中用户可以登录

需要检查:
- `auth_routes.py` 中的登录逻辑
- 前端登录表单与 API 的参数匹配

### 2. Admin 用户登录失败

RBAC 测试中 admin 用户登录失败:
```
[FAIL] Failed to login admin
```

### 3. RBAC 权限不匹配

RBAC 测试显示 8/24 项失败:
- `TEAM_LEADER -> order:create: expected=ALLOW, actual=DENY (status=400)`
- `KEEPER -> keeper_confirm: expected=DENY, actual=ALLOW (status=400)`
- `PRODUCTION_PREP -> transport_execute: expected=ALLOW, actual=DENY (status=400)`

---

## 下一步工作

### P0 修复 (达到8/10前必须修复)

1. **调查登录 API 不一致问题**
   - 检查 smoke test 和 RBAC test 的登录调用差异
   - 确保 API 响应一致

2. **修复 Admin 用户登录**
   - 验证 admin 账号凭据
   - 检查 RBAC 配置

3. **分析 RBAC 权限不匹配**
   - 24项测试中10项通过，8项失败
   - 需要确认权限矩阵是否正确

### P1 改进

1. 添加测试隔离机制防止数据冲突
2. 优化感知系统的异常检测逻辑

---

## 结论

**RESULT: PARTIAL COMPLETE**

1. **历史验证**: 2026-03-30 的运行 `04dab6a6-5106-46da-8151-715fbdf8cc83` 达到了 **8/10** 标准
2. **当前状态**: 由于测试基础设施问题（已部分修复），无法在本次会话中完成完整的3轮回归测试
3. **已修复**: Playwright E2E 测试的路由问题
4. **待修复**: 登录 API 不一致、RBAC 权限不匹配

---

## 测试基础设施状态

| 组件 | 状态 | 备注 |
|------|------|------|
| Backend API | ✅ 正常 | login_name 参数正确 |
| Frontend | ✅ 正常 | 路由已更新 |
| Playwright E2E | ⚠️ 需修复 | 路由已修复，登录有问题 |
| API E2E | ⚠️ 需修复 | RBAC 权限不匹配 |
| Sensing Module | ⚠️ 需修复 | 数据库锁定问题 |

---

## 归档状态

- 提示词文件: **未归档** (需完成完整3轮测试)
- 锁文件: **不适用**
- 测试路由修复: `playwright_e2e.py` 已更新
