# 工装出入库管理系统 - 产品需求文档 (PRD)

## 1. 系统概述

### 1.1 系统定位

工装出入库管理系统（Tool Inventory Inbound/Outbound Management System）是一个面向制造业内部的工装（工具/设备）库存管理模块。该系统主要用于管理工装的出库和入库生命周期，确保工装在生产过程中的流转可追踪、可控制。

### 1.2 核心价值

- 实现工装流转的电子化记录，替代纸质单据
- 明确各角色职责，实现责任可追溯
- 自动化生成业务文本，减少人工操作错误
- 集成飞书通知，实现即时消息触达

### 1.3 用户群体

| 角色 | 中文名称 | 主要职责 |
|------|----------|----------|
| Team Leader | 班组长 | 发起出库/入库申请，确认出库完成 |
| Keeper | 保管员 | 确认工装位置和状态，确认识入库完成 |
| Transport Operator | 运输操作员 | 接收运输通知，执行工装运输 |

---

## 2. 角色定义

### 2.1 班组长 (Team Leader)

- **权限范围**
  - 创建出库单（申请工装出库）
  - 创建入库单（申请工装归还）
  - 搜索工装库存
  - 查看自己发起的订单
  - 确认出库完成
  - 取消自己发起的订单

- **业务限制**
  - 只能操作自己创建的订单
  - 出库完成后需要确认收到
  - 入库由保管员确认完成

### 2.2 保管员 (Keeper)

- **权限范围**
  - 查看待确认的订单
  - 确认工装位置和状态
  - 拒绝订单
  - 生成运输通知文本
  - 发送飞书通知
  - 确认入库完成

- **业务限制**
  - 只能确认工装状态，不能修改工装基础数据
  - 入库完成后需要确认已入库

### 2.3 运输操作员 (Transport Operator)

- **权限范围**
  - 接收飞书通知（被动接收）
  - 查看运输通知文本（通过微信复制文本）

- **业务说明**
  - 运输操作员不直接操作系统，通过线下方式执行运输
  - 运输状态通过保管员确认更新

### 2.4 管理员 (Admin)

- **权限范围**
  - 查看所有订单
  - 查看操作日志
- 查看通知记录
- 管理系统配置

### 2.5 工程技术维护人员 (Engineering Maintainer)

- **权限范围**
  - 维护工装可拆卸件清单（MPL）
  - 查看工装图号与版次对应的组件号、数量和组件照片
  - 配合保管员完成组件完整性核对

- **业务说明**
  - 当前实现使用独立 `mpl:view` / `mpl:write` 权限控制 MPL 能力
  - `/mpl` 管理页仅对工程技术部和系统管理员开放
  - 保管员仅在确认链路中通过 `order:keeper_confirm` 间接读取 MPL，不开放管理页

---

## 3. 业务流程

### 3.1 出库流程 (Outbound Flow)

```
班组长创建出库单
        ↓
系统生成结构化申请文本
        ↓
保管员确认工装位置和状态
        ↓
系统生成运输通知文本
        ↓
发送飞书通知（可选）
        ↓
复制微信文本（线下通知运输）
        ↓
运输执行（线下）
        ↓
班组长确认收到 → 订单完成
```

### 3.2 入库流程 (Inbound Flow)

```
班组长创建入库单
        ↓
提交订单
        ↓
保管员确认工装状态和位置
        ↓
系统生成运输通知文本（可选）
        ↓
发送飞书通知（可选）
        ↓
运输执行（线下）
        ↓
保管员确认已入库 → 订单完成
```

---

## 4. 完成条件

### 4.1 出库完成条件

- 订单状态变更为 `completed`
- 班组长点击"确认收到"按钮
- 系统记录最终确认操作日志

### 4.2 入库完成条件

- 订单状态变更为 `completed`
- 保管员点击"确认入库"按钮
- 系统记录最终确认操作日志

---

## 5. 业务规则

### 5.1 订单创建规则

1. 出库单必须包含至少一个工装
2. 入库单必须包含至少一个工装
3. 订单号自动生成，保证唯一性
4. 创建时订单状态为 `draft`

### 5.2 订单状态流转规则

| 当前状态 | 可转向状态 | 触发操作 |
|----------|------------|----------|
| draft | submitted | 班组长提交 |
| submitted | partially_confirmed, keeper_confirmed, rejected | 保管员确认/拒绝 |
| partially_confirmed | keeper_confirmed, transport_notified, rejected | 保管员确认或发送运输通知 |
| keeper_confirmed | transport_notified, final_confirmation_pending, rejected | 发送运输通知或直接最终确认 |
| transport_notified | transporting | 开始运输 |
| transporting | final_confirmation_pending | 完成运输 |
| final_confirmation_pending | completed | 最终确认 |
| completed | - | 终态 |
| rejected | cancelled | 班组长取消 |
| cancelled | - | 终态 |

**完整状态说明：**

| 状态值 | 中文 | 说明 |
|--------|------|------|
| draft | 草稿 | 订单刚创建，未提交 |
| submitted | 已提交 | 已提交给保管员 |
| partially_confirmed | 部分确认 | 部分明细已确认 |
| keeper_confirmed | 保管员已确认 | 保管员已确认工装状态 |
| transport_notified | 已通知运输 | 已发送运输通知 |
| transporting | 运输中 | 运输进行中 |
| final_confirmation_pending | 待最终确认 | 等待最终确认 |
| completed | 已完成 | 订单流程结束 |
| rejected | 已拒绝 | 保管员拒绝 |
| cancelled | 已取消 | 已取消 |

### 5.3 文本生成规则

1. **保管员需求文本**：使用中文输出，包含部门、需求日期、工装清单、位置信息、确认要求
2. **运输通知文本**：使用中文输出，包含部门、需求日期、工装清单、运输目的地、时间要求
3. **数量展示规则**：通知明细优先展示 `split_quantity`，为空时回退到申请/确认数量
4. **微信复制文本**：使用中文输出的简化通知文本，便于微信分享

### 5.4 通知规则

1. 飞书通知可选择发送或不发送
2. 通知记录需要持久化存储
3. 通知失败需要记录错误信息

---

## 6. 异常场景

### 6.1 订单被拒绝

- 保管员可拒绝订单并填写拒绝原因
- 订单状态变更为 `rejected`
- 班组长可选择取消订单

### 6.2 订单取消

- 只有 `draft` 和 `rejected` 状态的订单可取消
- 取消后订单状态变更为 `cancelled`
- 已取消订单不可再操作

### 6.3 工装状态异常

- 工装在出库时记录当前状态快照
- 入库时保管员需确认工装状态
- 状态异常需记录在日志中

### 6.4 通知失败

- 飞书通知失败时记录错误信息
- 支持重试机制
- 用户可查看通知发送状态

### 6.5 并发操作

- 同一订单不允许同时操作
- 需要考虑数据库事务一致性

### 6.6 MPL 校验

- 支持为工装图号 + 版次维护可拆卸件清单（MPL）
- 保管员确认前可按系统开关执行 MPL 校验
- `mpl_enabled = false` 时，保管员确认流程不受影响
- `mpl_enabled = true` 且 `mpl_strict_mode = false` 时，缺少 MPL 仅提示警告
- `mpl_enabled = true` 且 `mpl_strict_mode = true` 时，缺少 MPL 阻止确认
- MPL 图片以前端 Base64 上传，单张限制 2MB

---

## 7. V1 范围 (MVP)

### 7.1 核心功能

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 出库单创建 | 班组长创建出库申请 | P0 |
| 入库单创建 | 班组长创建入库申请 | P0 |
| 订单提交 | 将订单提交给保管员 | P0 |
| 保管员确认 | 保管员确认工装位置状态 | P0 |
| 最终确认 | 出库由班组长确认，入库由保管员确认 | P0 |
| 订单拒绝 | 保管员拒绝订单 | P0 |
| 订单取消 | 班组长取消订单 | P1 |
| 运输通知生成 | 生成通知文本 | P1 |
| 飞书通知 | 发送飞书消息 | P1 |
| 订单列表 | 查看订单列表 | P0 |
| 工装搜索 | 搜索工装库存 | P0 |
| MPL 管理 | 维护工装可拆卸件清单 | P1 |
| MPL 校验开关 | 管理员配置 MPL 校验策略 | P1 |

### 7.2 不包含功能

- 复杂审批流程
- 高级RBAC权限系统
- 自动调度
- 多仓库物流优化
- 移动端支持

---

## 8. 未来扩展

### 8.2 期中扩展

- 工装二维码/条码扫描
- 工装保养提醒
- 工装库存预警

### 8.3 远期扩展

- 多仓库支持
- 工装借用预约
- 工装使用统计报表
- 移动端应用
- 与ERP系统集成

---

## 9. 术语表

| 术语 | 英文 | 定义 |
|------|------|------|
| 出库 | Outbound | 工装从仓库借出到生产现场 |
| 入库 | Inbound | 工装从生产现场归还到仓库 |
| 工装 | Tool | 生产用的工具和设备 |
| 保管员 | Keeper | 仓库管理人员 |
| 班组长 | Team Leader | 生产班组负责人 |
| 订单状态 | Order Status | 订单的生命周期状态 |
| 明细状态 | Item Status | 订单中单个工装的状态 |

---

## 10. 附录

### 10.1 相关文档

- [架构文档](./ARCHITECTURE.md)

### 10.2 技术约束

- 系统为单体应用，不引入微服务
- 使用 SQL Server 数据库
- 使用 pyodbc 进行数据库访问
- 后端使用 Python + Flask
- 前端使用 Vue3 + Element Plus
## 定检任务管理

- 系统需要提供定检计划、定检任务、检测报告、出入库联动的完整后端能力。
- 定检任务状态机覆盖 `pending -> received -> outbound_created -> outbound_completed -> in_progress -> report_submitted -> accepted/rejected -> inbound_created -> inbound_completed -> closed`。
- 检测报告附件使用 Base64 传输，单附件大小限制 2MB。
- 任务关键节点需要同步记录内部通知，并复用飞书通知通道。
